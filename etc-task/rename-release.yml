---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/concourse-ci

inputs:
  - name: source-git
  - name: ci-git
  - name: version
  - name: pkg-out

outputs:
  - name: renamed-out
    path: source-git/src/PaasXpertPortal/target

run:
  path: ci-git/etc-task/rename-release-exe.sh

